name: telco-churn-training

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: str, default: "namadataset_preprocessing/Telco-Customer-Churn_preprocessing.csv"}
    command: "python modelling.py --data_path {data_path}"
